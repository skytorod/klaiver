<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.klaiverjoin.mappers.LoginMapper">
	<select id="loginCheck" resultType="LoginVO">
		select email, password from k_joinone where email = #{email} and password = #{password}
	</select>
	
</mapper>